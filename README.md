```mermaid
graph TD
    subgraph 模块一：表型与基因组数据预处理
        A1(两年田间表型数据) --> A3[空间校正与计算]
        A2(全基因组芯片测序) --> A4[基因型质控与插补]
    end

    subgraph 模块二：GWAS 降维与特征初筛
        A3 --> B1{交叉验证划分}
        A4 --> B1
        B1 --> B2[仅训练集执行 GWAS]
        B2 --> B3(获取 Top-K SNP 集合)
    end

    subgraph 模块三：图卷积神经网络 GCN 构建
        B3 --> C1[定义节点 Nodes]
        B3 --> C2[定义边 Edges]
        C1 --> C3{构建基因组网络图}
        C2 --> C3
        C3 --> C4[多层图卷积网络]
        C4 --> C5((输出表型预测值))
    end

    subgraph 模块四：模型评估
        C5 --> D1[对比经典模型]
        C5 --> D2[对比机器学习]
        D1 --> D3[评估准确率与RMSE]
        D2 --> D3
    end

    subgraph 模块五：特征逆向解析与育种落地
        C4 -.可解释性分析.-> E1[权重溯源]
        E1 --> E2[挖掘核心 SNP]
        E2 --> E3[开发功能型分子标记]
        E3 --> E4(((育种群体田间验证)))
    end
```
